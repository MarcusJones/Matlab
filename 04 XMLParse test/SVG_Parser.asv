svgFile = 'Y:\04 Reports\Air Flow SVG\Temp7.svg'
test = xml_load(svgFile,'off')
V = xml_parseany(fileread('Y:\04 Reports\Air Flow SVG\TempNew.xml'))

V = xmlread('Y:\04 Reports\Air Flow SVG\TempNew.xml')

perlFile='"D:\L Scripts\03L Perl\04 Perl\01 Utilities\Directory Listing.pl"';

perl(perlFile)

perlCmd = ['perl ' perlFile]

[s r] = dos(perlCmd)
r


%% 
create_blank_SVG()

perlScriptPath = '.\lib\SVG_parse_test.pl';

svgTemplateFile = 'EmptyTemplate.svg';

svgNewOutputFile = 'New

perl([perlScriptPath svgTemplateFile])